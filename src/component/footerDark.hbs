{{#with data}}
<footer>
    <div class="footer_links">
        {{#each pages}}
        <a href="{{path}}" class="footer_link_item">{{name}}</a>
        {{/each}}
    </div>
    <div class="copyright">
        {{copyright}}
    </div>
</footer>
{{/with}}